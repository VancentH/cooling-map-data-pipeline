# ----------------------------------------------------------------------
# Local environment variables (copy .env.example → .env to configure)
# ----------------------------------------------------------------------
.env


# ----------------------------------------------------------------------
# Fetched data (not committed — ODbL share-alike, stored locally only)
# ----------------------------------------------------------------------
data/
